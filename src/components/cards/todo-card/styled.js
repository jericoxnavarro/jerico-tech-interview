import { Stack, styled } from '@mui/material';

const Container = styled(Stack)(() => ({
  border: '1px solid',
  borderColor: 'silver',
  borderRadius: '8px',
  flexDirection: 'row',
  padding: '12px',
  alignItems: 'center',
}));

export { Container };
